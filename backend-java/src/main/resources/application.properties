# Server Configuration
server.port=8080
spring.application.name=resume-builder

# ============================
# Database Configuration
# ============================

# H2 In-Memory Database (No installation needed - for testing)
spring.datasource.url=jdbc:h2:mem:resume_db
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect

# OPTION 2: PostgreSQL (Requires PostgreSQL installed)
# Uncomment these 4 lines and comment H2 above:
# spring.datasource.url=jdbc:postgresql://localhost:5432/resume_db
# spring.datasource.username=postgres
# spring.datasource.password=postgres
# spring.datasource.driver-class-name=org.postgresql.Driver
# spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# OPTION 3: MySQL (Requires MySQL installed)
# Uncomment these 4 lines and comment H2 above:
# spring.datasource.url=jdbc:mysql://localhost:3306/resume_db?createDatabaseIfNotExist=true
# spring.datasource.username=root
# spring.datasource.password=root
# spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ============================
# JWT Configuration
# ============================

jwt.secret=your-secret-key-change-in-production-23456789
jwt.expiration=604800000

# ============================
# Gemini AI Configuration
# ============================

gemini.api.key=${GEMINI_API_KEY:AIzaSyCSfDnHMrqDY-Guy8DTXYuLh3Ah-aQMPd0}
gemini.api.url=https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash:generateContent

# ============================
# CORS Configuration
# ============================

# Allow both local and production URLs
cors.allowed.origins=http://localhost:3000,https://ai-resume-55kl.onrender.com

# ============================
# Logging
# ============================

logging.level.com.resumeai=INFO
logging.level.org.springframework.security=DEBUG
